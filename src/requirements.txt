# Core LangGraph and LangChain Dependencies
langgraph>=0.0.55
langchain>=0.1.0
langchain-core>=0.1.0
langchain-openai>=0.1.0

# Configuration and validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# MCP client for external service integration
mcp>=1.0.0
httpx>=0.24.0

# Backup API clients (if MCP unavailable)
PyGithub>=1.59.0

# Claude Code SDK - available on PyPI
claude-code-sdk

# Anthropic API for fallback
anthropic>=0.25.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0

# Existing project dependencies (add your existing ones here)
# Add any other dependencies your categorisation module needs

# Utilities
pathlib>=1.0.0